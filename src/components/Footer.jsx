import React from "react";
import "./footer.css";
import IconAnimated from "./IconAnimated";

const Footer = () => {
  return (
    <footer>
      <IconAnimated icon={0} />
      <IconAnimated icon={1} />
    </footer>
  );
};

export default Footer;
